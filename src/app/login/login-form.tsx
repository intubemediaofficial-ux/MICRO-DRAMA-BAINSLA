"use client";
import { useState } from "react";
export default function LoginForm() { const [identifier, setIdentifier] = useState(""); const [code, setCode] = useState(""); const [requested, setRequested] = useState(false); const [message, setMessage] = useState("");
  async function submit(event: React.FormEvent) { event.preventDefault(); const endpoint = requested ? "/api/auth/verify" : "/api/auth/request"; const response = await fetch(endpoint, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(requested ? { identifier, code } : { identifier }) }); const data = await response.json(); if (response.ok && !requested) { setRequested(true); setMessage("OTP sent. In dev, use 123456."); } else if (response.ok) location.href = "/"; else setMessage(data.error?.message ?? "Something went wrong"); }
  return <form onSubmit={submit} className="mt-8 space-y-4"><input required value={identifier} onChange={e => setIdentifier(e.target.value)} placeholder="Phone or email" className="w-full rounded-xl bg-zinc-800 p-4 outline-none ring-rose-500 focus:ring-2" /><input value={code} onChange={e => setCode(e.target.value)} placeholder={requested ? "6-digit OTP" : "OTP appears after request"} disabled={!requested} className="w-full rounded-xl bg-zinc-800 p-4 outline-none ring-rose-500 focus:ring-2" /><button className="w-full rounded-xl bg-rose-500 p-4 font-bold">{requested ? "Enter MicroDrama" : "Send OTP"}</button>{message && <p className="text-sm text-zinc-400">{message}</p>}</form>; }
