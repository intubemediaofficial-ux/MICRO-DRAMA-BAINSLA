import Link from "next/link";
import { getSession } from "@/server/auth";
import { prisma } from "@/server/db";
export default async function WalletPage() { const session = await getSession(); const user = session ? await prisma.user.findUnique({ where: { id: session.userId }, include: { transactions: { orderBy: { createdAt: "desc" }, take: 8 } } }) : null; const bundles = await prisma.coinBundle.findMany({ where: { isActive: true }, orderBy: { sortOrder: "asc" } }); return <div className="p-5 pb-24"><Link href="/" className="text-zinc-400">← Discover</Link><h1 className="mt-7 text-3xl font-black">Your wallet</h1>{!user ? <div className="mt-8 rounded-2xl bg-zinc-900 p-5"><p>Sign in to manage coins and daily rewards.</p><Link href="/login" className="mt-4 inline-block rounded-full bg-rose-500 px-5 py-3 font-bold">Sign in</Link></div> : <><div className="mt-6 rounded-3xl bg-gradient-to-br from-amber-400 to-orange-600 p-6 text-zinc-950"><p className="text-sm font-bold uppercase">Available coins</p><p className="mt-2 text-5xl font-black">🪙 {user.coinBalance}</p><form action="/api/checkin" method="post"><button className="mt-5 rounded-full bg-zinc-950 px-5 py-3 font-bold text-white">Claim daily check-in</button></form></div><h2 className="mt-8 text-xl font-bold">Top up</h2><div className="mt-3 grid grid-cols-2 gap-3">{bundles.map(bundle => <form action="/api/purchases" method="post" key={bundle.id} className="rounded-2xl bg-zinc-900 p-4"><input type="hidden" name="bundleId" value={bundle.id} /><p className="text-2xl font-black">🪙 {bundle.coins + bundle.bonusCoins}</p><p className="text-sm text-zinc-400">₹{(bundle.priceMinor / 100).toFixed(0)} · Dev checkout</p><button className="mt-4 rounded-full bg-rose-500 px-4 py-2 text-sm font-bold">Buy coins</button></form>)}</div><h2 className="mt-8 text-xl font-bold">Recent activity</h2><div className="mt-3 space-y-2">{user.transactions.map(tx => <div key={tx.id} className="flex justify-between rounded-xl bg-zinc-900 p-3 text-sm"><span>{tx.type.replaceAll("_", " ")}</span><b className={tx.delta > 0 ? "text-emerald-400" : "text-rose-400"}>{tx.delta > 0 ? "+" : ""}{tx.delta}</b></div>)}</div></>}</div>; }
