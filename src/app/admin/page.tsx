import Link from "next/link";
import { getSession } from "@/server/auth";
import { prisma } from "@/server/db";
export default async function AdminPage() { const session = await getSession(); if (!session || session.role !== "ADMIN") return <div className="p-8"><h1 className="text-3xl font-black">Admin access required</h1><p className="mt-2 text-zinc-400">Sign in with an ADMIN account to manage the catalogue.</p><Link href="/login" className="mt-5 inline-block rounded-full bg-rose-500 px-5 py-3">Sign in</Link></div>; const [series, users, tx] = await Promise.all([prisma.series.count(), prisma.user.count(), prisma.coinTransaction.count()]); return <div className="p-5 pb-24"><p className="text-xs uppercase tracking-widest text-rose-400">CMS</p><h1 className="mt-2 text-4xl font-black">Command center</h1><div className="mt-8 grid grid-cols-3 gap-3">{[["Series", series], ["Users", users], ["Ledger rows", tx]].map(([label, value]) => <div className="rounded-2xl bg-zinc-900 p-4" key={String(label)}><p className="text-xs text-zinc-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></div>)}</div><div className="mt-8 grid gap-3 md:grid-cols-2"><Link href="/admin/series" className="rounded-2xl bg-zinc-900 p-5"><h2 className="font-bold">Series & episodes →</h2><p className="mt-2 text-sm text-zinc-400">Metadata, publish state, pricing and episode inventory.</p></Link><Link href="/admin/analytics" className="rounded-2xl bg-zinc-900 p-5"><h2 className="font-bold">Analytics →</h2><p className="mt-2 text-sm text-zinc-400">Funnel, revenue, consumption and genre metrics.</p></Link></div></div>; }
